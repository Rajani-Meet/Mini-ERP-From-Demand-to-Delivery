# Agent Rules

Mandatory:

- Use TypeScript only
- Use Next.js App Router
- Use Prisma ORM
- Use PostgreSQL
- Use Zod validation
- Use React Hook Form
- Use RBAC checks
- Create audit logs

Never:

- Use any type
- Skip validation
- Store secrets in code
- Use raw SQL
- Disable authentication