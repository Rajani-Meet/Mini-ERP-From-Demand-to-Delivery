# Strict Prohibitions

NEVER:

- Store passwords in plain text.
- Disable validation.
- Use any() types.
- Hardcode credentials.
- Delete audit records.
- Skip authorization checks.
- Expose internal IDs publicly.
- Return stack traces to users.
- Execute raw SQL without approval.
- Modify production data directly